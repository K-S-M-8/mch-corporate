import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Configured precisely to the strict NextJS 15+ Type structure
  devIndicators: {
    position: "bottom-right"
  }
};

export default nextConfig;